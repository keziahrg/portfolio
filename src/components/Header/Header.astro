---
import { Container } from '../Container'
import { Nav } from '../Nav'
import { Socials } from '../Socials'
import styles from './Header.module.css'
---

<header class={styles.header}>
    <Container width="lg">
        <div class={styles.header__content}>
            <span class={styles.header__logo}>Keziah</span>
            <Nav />
            <Socials />
        </div>
    </Container>
</header>
